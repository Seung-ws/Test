create table member(
	uid varchar(36) not null,
	gid varchar(36),	
	username varchar(20),
	password varchar(20),
	constraint member_uid_uk unique(uid),
	constraint member_username_pk primary key(username)	
);